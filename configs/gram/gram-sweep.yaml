
# WANDB_AGENT_DISABLE_FLAPPING=true WANDB_AGENT_MAX_INITIAL_FAILURES=9000 wandb agent mscai-spygeorgoulas/iclr_gram_2026/urbbl1zg

program: /home/intern/spygeorgoulas/thesis-metanets/scalegmn/scalegmn_inclr_2026_gram.py
method: random

metric:
  name: val/challenge_metric
  goal: minimize

parameters:
  conf:
    values:
      - configs/gram/scalegmn.yml

  batch_size:
    values:
      #- 4
      #- 8
      - 16
      - 32
      - 64
      - 128

  scalegmn_args.d_hid:
    values:
      #- 16
      - 32
      - 64
      - 128
  
  scalegmn_args.direction:
    values:
      - bidirectional
      #- forward

  scalegmn_args.num_layers:
    values:
      #- 3
      #- 4
      - 6
      - 8
      #- 10

  scalegmn_args.gnn_args.dropout:
    values:
      #- 0.01
      #- 0.001
      - 0.1

  optimization.optimizer_args.lr:
    values:
      #- 2e-4
      #- 5e-4
      #- 1e-3
      - 1e-4

  optimization.optimizer_args.weight_decay:
    values:
      #- 1e-3
      - 1e-5
    
  wandb:
    values:
      - True